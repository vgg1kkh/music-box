import React, { memo } from 'react'

import DiscoverWrapper from './style'

export default memo(function Discover() {
    return (
        <DiscoverWrapper>
            <div className="sub-nav w980">Subnav</div>
        </DiscoverWrapper>
    )
})
