import React, { memo } from 'react'

import { PlayListWrapper } from './style'

export default memo(function PlayList() {
    return (
        <PlayListWrapper>
            Playlist
        </PlayListWrapper>
    )
})
