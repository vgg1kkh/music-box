import React, { memo } from 'react'

import DiscoverWrapper from './style'

export default memo(function Discover() {
    return (
        <DiscoverWrapper>
            <div className="sub-nav">Discover Nav</div>
        </DiscoverWrapper>
    )
})
