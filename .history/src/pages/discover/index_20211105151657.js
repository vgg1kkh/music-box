import React, { memo } from 'react'

import DiscoverWrapper from './style'

export default memo(function Discover() {
    return (
        <DiscoverWrapper>
            <div>Discover Nav</div>
        </DiscoverWrapper>
    )
})
