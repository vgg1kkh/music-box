import styled from "styled-components"

const MineWrapper = styled.div`
.content{
    h
    background: url(${require("@/assets/img/mymusic.png").default}) 0 0 no-repeat;
}
`

export default MineWrapper